import {ParametricIcon} from "/node_modules/parametric-icon/index.js"
import {template} from "../templates/RoutePerspective.js"

/**
 * @class RoutePerspective
 * @extends ParametricIcon
 * @classdesc 
 * 
 * A parameterizable icon, use is with the icon-route-perspective html tag. 
 * All the parameter of this class can also be passed through html attribute.
 * 
 * 
 */
class RoutePerspective extends ParametricIcon {

	connectedCallback(){
		this.setStyle(":host{display:block}")
		super.connectedCallback()
	}
	static get shadowMode(){
		return "open"
	}
	static get imageParameters() {
		return []
	}
	static get serialize() {
		return this.standardSerial
	}
	static get template(){
		return template
	}
}
export default RoutePerspective
