import {ParametricIcon} from "/node_modules/parametric-icon/index.js"
import {template} from "../templates/AirQuality1.js"

/**
 * @class AirQuality1
 * @extends ParametricIcon
 * @classdesc 
 * 
 * A parameterizable icon, use is with the icon-air-quality1 html tag. 
 * All the parameter of this class can also be passed through html attribute.
 * 
 * @property {string} offsetheight How empty space to leave bellow the icon. The unit is the icon own height.
 * @property {string} fill an image parameter.
 * @property {string} stroke an image parameter.
 * @property {string} pollutant an image parameter.
 * @property {string} value an image parameter.
 */
class AirQuality1 extends ParametricIcon {

	connectedCallback(){
		this.setStyle(":host{display:block}")
		super.connectedCallback()
	}
	static get shadowMode(){
		return "open"
	}
	static get imageParameters() {
		return ["offsetheight","fill","stroke","pollutant","value"]
	}
	static get serialize() {
		return this.standardSerial
	}
	static get template(){
		return template
	}
}
export default AirQuality1
